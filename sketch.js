let c = new Cartesian({});
// c.colorPallete = {
//     background : c.sketch.color(120, 150, 80),
//     axis : 30,
//     markings : 40,
//     grid : c.sketch.color(20, 60, 40)
// }
// c.addPoint(3,2);
c.draw(() => {
    // c.zoomToCenter(1, 2).then(() =>{
    //     c.panTo([0, 3]).then(() => {
    //         c.zoomToCenter(-1, 10);
    //     });
    // });
});